View the repository status

git status

git pull to retrieve the rest

Shows modified, added, or untracked files

Add a file to track
git add filename.txt

(To add everything: git add .)

git commit -m "Clear description of the change"

git push

TO MERGE, SEND DEV INTO MAIN
git switch main
git merge dev -m "v1.1"
git push